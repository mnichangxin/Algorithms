import sys


def adjustHeap(arr, i, lenth):
    k = arr[i]
    for i in 

def heapSort(arr):
    for i in range(len(arr) // 2 - 1, -1, -1):    
    

for line in sys.stdin:
    arr = list(map(int, line.split()))
    print(heapSort(arr))
